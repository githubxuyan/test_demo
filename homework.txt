全局路由和子路由的作用
全局路由，多个应用的分类
子路由，如果多个应用下有多个路由，子路由可以分门别类，主路由urls里urlpatterns列表不会很乱

路由匹配规则
1.从列表的第一个元素开始（从上到下）开始匹配
2.一旦匹配成功，会自动调用path第二个参数所指定的视图函数
3.一旦匹配成功之后，不会再往下匹配
4.如果匹配不成功，会返回一个状态码为404的页面
5.url路由信息，推荐使用/结尾